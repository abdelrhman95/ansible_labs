[defaults]
inventory = /home/abdo/ansible/day1/inventory
private_key_file = ~/.ssh/devops
remote_user = abdo


[privilege_escalation]
become = true
become_ask_pass = true
become_method = sudo
become_user = root